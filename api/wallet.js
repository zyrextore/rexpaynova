import {supabase,getUser} from "../lib/supabase.js";
export default async function handler(req,res){if(req.method!=="GET")return res.status(405).json({success:false,message:"Method not allowed"});const user=await getUser(req);if(!user)return res.status(401).json({success:false,message:"Unauthorized"});const {data,error}=await supabase.from("wallets").select("id,balance,created_at,updated_at").eq("user_id",user.id).single();if(error)return res.status(500).json({success:false,message:"Wallet error"});res.json({success:true,data});}